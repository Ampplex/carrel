# secrets — never commit
.env
*.env
!.env.example

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# runtime state: photo display cache, quota ledger
backend/var/

# node
node_modules/
dist/
.vite/

# os
.DS_Store

# typescript incremental build cache
*.tsbuildinfo

# expo
mobile/.expo/
mobile/dist/
.expo/

# Android release signing key — a private key; never committed.
mobile/android/app/*.keystore
!mobile/android/app/debug.keystore

# Play upload bundle — 51MB build artefact, rebuilt with `./gradlew bundleRelease`
play/bundle/


# Admin console credentials (Postgres URL + Neo4j Aura password)
admin/.env
